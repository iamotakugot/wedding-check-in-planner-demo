# 🔐 คู่มือการล็อกอินแอดมิน - Admin Login Guide

## 📋 สารบัญ

1. [ข้อมูลการล็อกอิน](#1-ข้อมูลการล็อกอิน)
2. [วิธีล็อกอิน](#2-วิธีล็อกอิน)
3. [แก้ปัญหาการล็อกอิน](#3-แก้ปัญหาการล็อกอิน)
4. [สร้าง Admin Account ใหม่](#4-สร้าง-admin-account-ใหม่)

---

## 1. ข้อมูลการล็อกอิน

### Admin Account ปัจจุบัน
- **Email**: `admin@admin.com`
- **Password**: `11501150`
- **UID**: `31y6CzqUO2aVj0VmLZAqg2LL1tJ3`

### URL
- **Admin Panel**: `http://localhost:5173/admin` (Development)
- **Admin Panel**: `https://got-nan-wedding.web.app/admin` (Production)

---

## 2. วิธีล็อกอิน

1. เปิดหน้า Admin Panel
2. กรอก **Email**: `admin@admin.com`
3. กรอก **Password**: `11501150`
4. คลิก **เข้าสู่ระบบ**

---

## 3. แก้ปัญหาการล็อกอิน

### ❌ ปัญหา: "บัญชีนี้ไม่มีสิทธิ์เข้าถึง Admin Panel"

**สาเหตุ:** UID ของ user ยังไม่ได้ถูกเพิ่มใน Firebase Realtime Database

**วิธีแก้:**

1. เปิด Firebase Console: https://console.firebase.google.com
2. เลือกโปรเจกต์: `got-nan-wedding`
3. ไปที่ **Realtime Database** → **Data**
4. สร้าง node `admins` (ถ้ายังไม่มี)
5. เพิ่ม child node ชื่อ `31y6CzqUO2aVj0VmLZAqg2LL1tJ3` และตั้งค่าเป็น `true`

**โครงสร้างที่ถูกต้อง:**
```
admins
  └── 31y6CzqUO2aVj0VmLZAqg2LL1tJ3: true
```

6. ลองล็อกอินใหม่

### ❌ ปัญหา: "ไม่พบผู้ใช้นี้ในระบบ"

**สาเหตุ:** Email ไม่ถูกต้อง หรือยังไม่ได้สร้าง user ใน Firebase Authentication

**วิธีแก้:**

1. ตรวจสอบ Email ใน Firebase Console → Authentication → Users
2. ใช้ Email ที่ถูกต้อง

### ❌ ปัญหา: "รหัสผ่านไม่ถูกต้อง"

**สาเหตุ:** รหัสผ่านผิด

**วิธีแก้:**

1. ตรวจสอบรหัสผ่านใน Firebase Console
2. หรือ Reset Password:
   - ไปที่ Authentication → Users
   - คลิกที่ user
   - คลิก "Reset password"

### ❌ ปัญหา: "Error checking admin status"

**สาเหตุ:** Security Rules บล็อกการอ่าน `/admins`

**วิธีแก้:**

1. ตรวจสอบ Security Rules ใน [FIREBASE_RULES.md](./FIREBASE_RULES.md)
2. ต้องมี Rules ที่อนุญาตให้อ่าน `/admins/{uid}` ของตัวเอง:
   ```json
   "admins": {
     "$uid": {
       ".read": "auth != null && auth.uid === $uid"
     }
   }
   ```

---

## 4. สร้าง Admin Account ใหม่

### ขั้นตอนที่ 1: สร้าง User ใน Firebase Authentication

1. ไปที่ Firebase Console: https://console.firebase.google.com
2. เลือกโปรเจกต์: `got-nan-wedding`
3. ไปที่ **Authentication** → **Users**
4. คลิก **Add user** (หรือ **Add user manually**)
5. กรอกข้อมูล:
   - **Email**: ใส่ email ที่ต้องการใช้เป็น admin
   - **Password**: ตั้งรหัสผ่านที่แข็งแรง (อย่างน้อย 6 ตัวอักษร)
6. คลิก **Add user**

### ขั้นตอนที่ 2: คัดลอก UID

1. หลังจากสร้าง user แล้ว จะเห็นรายการ user ในตาราง
2. คลิกที่ user ที่เพิ่งสร้าง
3. **คัดลอก UID** (จะเป็น string ยาวๆ)
4. **เก็บ UID นี้ไว้** - จะใช้ในขั้นตอนต่อไป

### ขั้นตอนที่ 3: เพิ่ม UID ใน Database

1. ไปที่ **Realtime Database** → **Data**
2. คลิกที่ node **`admins`**
3. คลิก **+** (Add node)
4. ใส่ชื่อ node: **UID ที่คัดลอกไว้**
5. ใส่ค่า: **`true`**
6. คลิก **Add**

### ขั้นตอนที่ 4: ทดสอบ

1. ล็อกอินด้วย Email และ Password ที่สร้างไว้
2. ควรจะเข้าสู่ระบบได้และเห็น Dashboard

---

## ✅ Checklist

- [ ] สร้าง Admin Account ใน Firebase Authentication แล้ว
- [ ] คัดลอก Admin UID ไว้แล้ว
- [ ] เพิ่ม Admin UID ใน Database (`/admins/{UID}` = `true`) แล้ว
- [ ] ทดสอบล็อกอินได้แล้ว

---

## 📝 หมายเหตุสำคัญ

1. **เก็บ Email และ Password ของ Admin ไว้ในที่ปลอดภัย**
2. **อย่าแชร์ Admin UID ให้คนอื่น**
3. **ถ้าต้องการเพิ่ม Admin คนอื่น:** ทำตามขั้นตอนที่ 4
4. **ถ้าลืมรหัสผ่าน:** ใช้ฟีเจอร์ "Reset password" ใน Firebase Console

---

**อัพเดทล่าสุด:** 2024

